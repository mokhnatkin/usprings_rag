# Прогресс разработки MVP1

Журнал выполнения `mvp-dev-plan.md` (MVP1). Обновляем по ходу реализации: статус
этапа, что фактически сделано, результат проверки приёмки, всплывшие вопросы
(со ссылкой на `open-questions.md`). Решения по калибровкам и открытым вопросам
пишем в `open-questions.md`, сюда - краткий факт со ссылкой.

Статусы: `не начат` / `в работе` / `сделано` / `заблокирован`.

## Сводка по этапам

| Этап | Название | Статус | Дата | Примечание |
|-|-|-|-|-|
| 1 | Коллекции в БД (фундамент) | сделано | 2026-07-15 | миграция 0004 + read-model; alembic check чист; 27 тестов |
| 2 | Аутентификация | сделано | 2026-07-15 | argon2, cookie-сессии, bootstrap; 36 тестов |
| 3 | Авторизация и доступ к коллекциям | сделано | 2026-07-15 | RBAC, гранты, /ask 403, смена пароля; 42 теста |
| 4 | Логирование вопросов-ответов | сделано | 2026-07-15 | query_log, полный текст, токены, отказы; 47 тестов |
| 5 | Обратная связь по ответу | сделано | 2026-07-15 | /feedback (только своя запись), кнопка+комментарий; 53 теста |
| 6 | История вопросов пользователя | не начат | - | последние 3 на главной + полная с пагинацией |
| 7 | Инструкции: загрузка и soft-delete | не начат | - | фоновая индексация со статусом, archived_at |
| 8 | Справочники super-admin | не начат | - | пользователи, коллекции, просмотр лога |
| 9 | Калибровка порогов | не начат | - | запуск eval из UI, порог в БД без деплоя |
| 10 | Аналитика (базовые агрегаты) | не начат | - | поверх query_log, разрез коллекция/портал |
| 11 | UI и навигация | не начат | - | собирается параллельно с этапами |
| 12 | Упаковка и документация | не начат | - | .env.example, README, docs (блок M) |

## Незакрытые хвосты (повторить позже)

Пункты, которые НЕ выполнены и не должны потеряться. Убираем отсюда только после
фактического выполнения.

- Пусто (работа по MVP1 ещё не начата).

## Журнал (новые записи сверху)

<!--
Формат записи:

### YYYY-MM-DD. Этап N - краткий заголовок
- Статус: сделано / в работе / заблокирован
- Что сделано: ...
- Проверка приёмки: результат ...
- Вопросы/решения: см. open-questions.md (пункт ...)
-->

### 2026-07-15. Этап 5 - Обратная связь по ответу
- Статус: сделано.
- Что сделано:
  - `logging_qa.log_query` теперь возвращает id созданной строки; `set_feedback`
    (feedback=True, feedback_at=now, feedback_comment) - только для своей записи
    (чужая/несуществующая -> False).
  - `log_id` прокинут в ответ: `AskResponse.log_id` и событие `done` SSE (в стриме
    логируем ДО события done, чтобы вложить id).
  - `POST /feedback` (`{log_id, comment?}`): своя запись -> 200; чужая/нет -> 404;
    без сессии -> 401.
  - UI: блок «Ответ неверный» под ответом; клик открывает опциональный комментарий
    и «Отправить»; после отправки - «Спасибо, отмечено». Кнопка появляется только
    если строка лога создалась (есть `log_id`).
- Проверка приёмки (тесты `test_feedback.py`, 53 всего):
  - `set_feedback`: своя запись -> True (feedback/comment/at проставлены); чужая ->
    False (не тронута); несуществующая -> False.
  - Эндпоинт: своя запись -> 200 и отметка в БД; чужая -> 404; без сессии -> 401.
  - `node --check app.js` - синтаксис ок.
- Заметки:
  - Отметка возможна и для отказа (у него тоже есть строка лога и log_id) - ложный
    отказ это валидный сигнал.
  - Живой клик в браузере не гонял (нужен полный портал с LLM); поток «ответ ->
    log_id -> /feedback» покрыт эндпоинт-тестами.

### 2026-07-15. Этап 4 - Логирование вопросов-ответов
- Статус: сделано.
- Что сделано:
  - Модель `QueryLog` + миграция 0007 (полный текст вопроса/ответа, outcome,
    best_similarity, prompt/completion/total токены, elapsed, model_id, sources JSONB,
    поля feedback_* под этап 5; индексы по (user_id, created_at) и
    (collection_id, created_at)).
  - Протянут usage: `Answer` получил `model_id`/`prompt_tokens`/`completion_tokens`
    (у отказа нули); `answer_question` берёт их из `LLMResponse`; `stream_generate`
    пишет расход в usage-словарь, `stream_answer` кладёт его в итоговый `Answer`.
  - В `Collection` добавлен `id` (для FK лога), заполняется read-model и сервисом.
  - `logging_qa.log_query`: запись строки, best-effort (try/except + rollback, сбой
    не роняет ответ). Вызывается из `/ask` (полный текст в `answer.text`) и из
    `/ask/stream` (текст собирается из дельт, передаётся `answer_text`).
- Проверка приёмки (тесты `test_logging.py` + `test_stream.py`, 47 всего):
  - answered-строка: полный вопрос/ответ, collection_id, best_similarity, токены
    (120/40/160), model_id, sources=[{document_id,title}].
  - refused-строка: `outcome=refused`, токены 0/0/0, model пустой.
  - Стрим: usage из фейкового `stream_generate` доезжает до `Answer`; `answer_text`
    перекрывает пустой `text`.
  - Сбой вставки (collection.id=None -> NOT NULL) не пробрасывается наружу, строка
    не создаётся.
- Заметки:
  - Сквозной `/ask` с реальной LLM не гонял (нужен OpenRouter + BGE-m3); запись лога
    покрыта юнит-тестами той же функции `log_query`, что вызывают эндпоинты.
  - `QUERY_LOG_PREVIEW_CHARS` (усечение для списков) применится в выборках истории
    и аналитики (этапы 6, 10) - в самой записи храним полный текст.

### 2026-07-15. Этап 3 - Авторизация и доступ к коллекциям (RBAC)
- Статус: сделано.
- Что сделано:
  - Модель `UserCollectionAccess` (составной PK user_id+collection_id, FK cascade) +
    миграция 0006.
  - `auth.py`: `accessible_codes` (super_admin - все активные; иначе гранты x активные
    коллекции), `check_collection_access` (403; `need_admin` - для управляющих операций
    этапов 7+), `change_password` (по верному старому паролю).
  - `api.py`: `/collections` фильтруется по доступу; `/ask` и `/ask/stream` проверяют
    доступ (403 до вызова LLM, у стрима - до начала SSE); `GET /profile`,
    `POST /profile/password`, форма `templates/profile.html`; ссылка «Профиль» в шапке.
- Проверка приёмки (тесты `test_access.py`, 42 всего):
  - `user` с грантом только на `erp`: `accessible_codes` = {erp}; `/collections`
    отдаёт только erp; `/ask` и `/ask/stream` с `zup` -> 403.
  - super_admin: `accessible_codes` = все активные ({erp, zup}).
  - Смена пароля: неверный старый -> `error`, верный -> `changed`, новый пароль
    работает при следующем входе.
- Заметки:
  - `require_role` (зависимость по роли) отложена до этапа 7 - там появится первый
    потребитель (админ-эндпоинты); мёртвый код сейчас не заводим.
  - CSRF для изменяющих форм (login/logout/profile) - отдельной задачей; сейчас
    полагаемся на SameSite=Lax. Ужесточить при появлении админ-действий.

### 2026-07-15. Этап 2 - Аутентификация
- Статус: сделано.
- Что сделано:
  - Модель `User` (`models.py`) + enum `Role` (user / collection_admin / super_admin);
    миграция 0005 (таблица users, без сида - учётка создаётся bootstrap).
  - `security.py`: `hash_password`/`verify_password` на argon2 (`pwdlib[argon2]`).
  - `auth.py`: провайдер `authenticate` (внутренние учётки - сюда позже ляжет
    LDAP/AD, не трогая эндпоинты), сессия (`login_user`/`logout_user`,
    `current_user_or_none`, `get_current_user` - зависимость 401), `bootstrap_super_admin`.
  - `api.py`: `SessionMiddleware` (SECRET_KEY, HttpOnly, SameSite=Lax; пустой ключ -
    эфемерный dev-режим с предупреждением); `GET/POST /login`, `POST /logout`,
    форма `templates/login.html`; bootstrap в lifespan; `/`, `/ask`, `/ask/stream`,
    `/collections` закрыты авторизацией (аноним на `/` -> редирект на `/login`).
  - Зависимости: `pwdlib[argon2]`, `itsdangerous` (сессии), `python-multipart` (формы).
  - Конфиг: `SECRET_KEY`, `SESSION_COOKIE_NAME`, `SESSION_MAX_AGE`, `SUPERADMIN_LOGIN`,
    `SUPERADMIN_PASSWORD` в `.env`/`.env.example`.
- Проверка приёмки:
  - Bootstrap идемпотентен: пустая таблица -> 1 super-admin; повторный вызов -> без
    дублей (проверено вживую: users 0 -> 1 -> 1; auth верный/неверный пароль).
  - Тесты (`test_security.py`, `test_auth.py`): аноним на `/` -> 303 `/login`;
    `/ask` и `/ask/stream` без сессии -> 401; вход верными кредами -> сессия и `/`
    открыт; логаут рвёт сессию; неверный пароль и неактивная учётка -> `/login?error`.
    TestClient без lifespan (BGE-m3 не грузится). Всего 36 тестов зелёных.
- Заметки:
  - `same_site=lax`, `https_only=false` - on-premise может работать по http; за TLS
    включить `https_only=True`. CSRF для админ-форм - на этапах с изменяющими
    действиями (3+).
  - Смена своего пароля (B6) и RBAC/доступ к коллекциям - этап 3.

### 2026-07-15. Этап 1 - Коллекции в БД (фундамент)
- Статус: сделано.
- Что сделано:
  - Модель `CollectionRow` + миграция 0004 (таблица `collections`), сид `erp` (0.58,
    its_erp) и `zup` (0.55, its_zup) из прежнего enum.
  - `collection.py` -> тонкая read-model поверх БД: `get_collection`/`list_collections`
    читают справочник (кэш в памяти, `invalidate_cache`); объект `Collection` тот же
    (`code`/`title`/`folder`/`threshold`), поэтому `retrieval.search`/`answer_*` не
    менялись. `CollectionCode`/`DEFAULT_COLLECTION` остались как константы для
    сида/дефолта, не как источник истины.
  - Потребители переведены на БД: `api.py` (`/collections`, `_resolve_collection` ->
    422 на неизвестной), `ingest/cli.py`, `search_cli.py`, `eval/run_eval.py` (убраны
    статические `choices`, валидация через `get_collection`).
  - `collections_service.create_collection`: строка + `CREATE TABLE ... PARTITION OF
    chunks` + папка, в одной транзакции (DDL Postgres транзакционен), с проверкой
    формата кода; сбрасывает кэш.
  - `alembic/env.py`: `include_object` игнорирует дочерние секции `chunks_<code>` -
    иначе autogenerate на каждую секцию предлагал бы drop_table (ложный дифф).
- Проверка приёмки:
  - `alembic check` - "No new upgrade operations detected" (модель = схема).
  - Тесты изоляции коллекций и логики поиска зелёные без изменения сигнатур `search`;
    новый `test_collections_service.py`: read-model видит сид, неизвестный код -
    ValueError, `create_collection` заводит строку + секцию (в плане запроса
    `chunks_<code>`) + папку. Всего 27 тестов.
  - `grep`: бизнес-логики на enum как источнике истины не осталось (только
    `DEFAULT_COLLECTION` и конструирование `Collection` в логических тестах).
- Заметки:
  - `code` коллекции неизменяем (имя секции `chunks_<code>`); правка кода запрещена,
    меняем title/threshold/is_active (админка - этап 8).
  - Логические тесты (`test_stream`, `test_collection_isolation`) конструируют
    `Collection` напрямую - не зависят от БД-справочника при импорте.

### 2026-07-15. Планирование MVP1 (backlog + dev-plan)
- Статус: сделано (планирование; разработка не начата).
- Заведены документы MVP1: `backlog.md` (направления, решения владельца, блоки
  задач A-M), `mvp-dev-plan.md` (детальный план, 12 этапов с приёмкой) и этот
  журнал прогресса.
- **Решения владельца (2026-07-15)**, зафиксированы в `backlog.md`:
  1. Аутентификация - **только внутренние учётки** (логин+пароль, cookie-сессии,
     bootstrap super-admin из `.env`). LDAP/AD - пост-MVP.
  2. Коллекции **переезжают из enum в БД** (таблица `collections`); super-admin
     ведёт справочник и калибрует пороги из UI.
  3. Загрузка инструкций - **PDF, фоновая индексация со статусом, soft-delete**
     (`documents.archived_at`), возврат из архива разрешён. Word - пост-MVP.
  4. Аналитика - **базовые агрегаты** (запросы/токены, доля отказов, доля
     «неверных», активность, топ вопросов).
  5. Тексты вопросов и ответов храним **полностью** (N - только усечение для
     списков/аналитики).
  6. Пользователь **сам меняет свой пароль**; отметка «ответ неверный» - с
     опциональным комментарием.
- **Порядок работ:** этап 1 (коллекции в БД) - первый и фундаментный, трогает
  горячий путь поиска; далее auth -> доступ -> логирование -> история -> админка ->
  аналитика -> упаковка.
- Открытых вопросов по плану MVP1 нет - все восемь остаточных решены владельцем
  2026-07-15. Открытыми на проекте остаются LDAP/AD и приватность LLM
  (zero-data-retention перед продом) - см. `open-questions.md`.
