# Прогресс разработки MVP

Журнал выполнения `mvp-dev-plan.md`. Обновляем по ходу реализации: статус этапа,
что фактически сделано, результат проверки приёмки, всплывшие вопросы (со ссылкой
на `open-questions.md`). Решения по калибровкам пишем в `open-questions.md`, сюда -
краткий факт со ссылкой.

Статусы: `не начат` / `в работе` / `сделано` / `заблокирован`.

## Сводка по этапам

| Этап | Название | Статус | Дата | Примечание |
|-|-|-|-|-|
| 1 | Каркас и окружение | сделано | 2026-07-10 | БД на порту 5433 (5432 занят) |
| 2 | Схема БД и миграции (SQLAlchemy + Alembic) | сделано | 2026-07-10 | alembic check чист |
| 3 | Ingest: парсинг и чанкинг | сделано | 2026-07-11 | pypdf справился со всеми 8; pdfplumber не понадобился |
| 4 | Ingest: эмбеддинги и запись в БД | сделано | 2026-07-11 | 8 документов, 23 чанка, dim 1024; повторный прогон без дублей |
| 5 | Семантический поиск + калибровка порога (eval, recall@k, SIMILARITY_THRESHOLD) | сделано | 2026-07-12 | recall@1 = 17/17; порог 0.53 по eval из 25 вопросов |
| 6 | Генерация ответа + A/B LLM | в работе | 2026-07-12 | 6.1-6.3 сделано и проверено; 6.4 (A/B) заблокирован 429 free-тира |
| 7 | Портал + раздача исходных PDF | сделано | 2026-07-12 | 7.1-7.4 сделано; 7.5 (SSE) - опционально, не делали |
| 8 | Упаковка и запуск с нуля | не начат | - | - |

## Незакрытые хвосты (повторить позже)

Пункты, которые НЕ выполнены и не должны потеряться. Убираем отсюда только после
фактического выполнения.

- **Подшаг 6.4 - A/B-сравнение LLM. НЕ ВЫПОЛНЕН (2026-07-12), повторить.**
  Обе плановые модели (`qwen/qwen3-next-80b-a3b-instruct:free` и
  `google/gemma-4-31b-it:free`) всю сессию отдавали `429 rate-limited upstream`,
  сравнить их не удалось. Что делать при повторе:
  1. Проверить доступность обеих моделей (дешёвый запрос к
     `https://openrouter.ai/api/v1/chat/completions`, ждём не 429).
  2. Прогнать на каждой: `uv run --no-sync python eval/run_answers.py --model <id>`
     (инструмент готов, набор вопросов внутри - покрытые, околодоменный, вне-доменные).
  3. Сравнить качество русского языка и следование правилу «только по контексту»
     (особенно честность «информации нет» на околодоменном вопросе).
  4. Зафиксировать выбор в `open-questions.md` и, если победит альтернатива,
     сменить `OPENROUTER_MODEL` в `.env` / `.env.example`.
  До этого `OPENROUTER_MODEL` остаётся прежним (`qwen3-next`) - молча не меняем.
  Приёмка этапа 6 в остальном пройдена (см. запись от 2026-07-12): сценарий проверен
  на временно доступной `google/gemma-4-26b-a4b-it:free`, но она **не кандидат** -
  выбор делаем между двумя плановыми моделями.
- Этап 6 закрывается только после выполнения 6.4; до этого статус - «в работе».

## Журнал (новые записи сверху)

<!--
Формат записи:

### YYYY-MM-DD. Этап N - краткий заголовок
- Статус: сделано / в работе / заблокирован
- Что сделано: ...
- Проверка приёмки: результат ...
- Вопросы/решения: см. open-questions.md (пункт ...)
-->

### 2026-07-12. Этап 7 - Портал и раздача PDF (7.1-7.4 сделано)
- Статус: сделано (7.5 - стриминг SSE - в плане помечен опциональным, не делали)
- Что сделано:
  - `templates/index.html` + `static/style.css` + `static/app.js`: экран вопрос-ответ,
    состояние «Думаю...», блок источников со ссылками, отказ - отдельным визуальным
    состоянием (жёлтая карточка), в подвале сходство и время. Шаблон отдаём как
    статический файл (`FileResponse`), Jinja2 не подключали: динамики в разметке нет,
    весь рендер на клиенте - не переусложняем.
  - `api.py`: `GET /` (экран), `/static` и `/manuals` через StaticFiles.
  - **7.4**: `MANUALS_DIR` в конфиге (по умолчанию `docs/manuals`);
    `pipeline.relative_source_path()` кладёт в БД путь относительно этой папки с
    POSIX-разделителями (`IT_1C/Отгрузка.pdf`). База пересобрана
    (`TRUNCATE documents CASCADE` + ingest, подтверждено владельцем): 8 документов,
    23 чанка, пути нормализованы.
  - Закрыт открытый вопрос этапа 6 (источники под ответом «информации нет»): системный
    промпт требует вернуть ровно маркер `НЕТ_ОТВЕТА`, `answer.py` превращает его в
    обычный отказ без источников. У пользователя ровно два исхода: ответ с источниками
    либо отказ без источников. Детали - `open-questions.md`.
  - Тесты: +2 на нормализацию пути (`tests/test_pipeline.py`), всего 15 зелёных.
- Проверка приёмки (HTTP-прогон против поднятого портала):
  - `GET /` - 200, форма на месте; `/static/style.css`, `/static/app.js` - 200.
  - Ссылка из ответа `GET /manuals/IT_1C/Оформление трудозатрат.pdf` - 200,
    `application/pdf`, 456 КБ. Листинг папки `/manuals/IT_1C/` - 404 (файлы отдаются,
    оглавление каталога - нет).
  - Сквозной сценарий: релевантный вопрос - ответ за 6 с, 3 источника со ссылками;
    околодоменный («инвентаризация склада», 0.5466 - выше порога) - `refused=true`,
    **0 источников** (сработал маркер); вне-доменный («погода») - отказ за 0.17 с
    без вызова LLM.
  - Прогон на `google/gemma-4-26b-a4b-it:free`: плановые модели всё ещё под 429
    (см. «Незакрытые хвосты», подшаг 6.4 - A/B по-прежнему ждёт).
- Грабли по ходу:
  - Точка входа `ingest` пропала («program not found») - консольные скрипты слетают
    после манипуляций с пакетом; лечится `uv pip install -e . --no-deps`.
  - Переустановка падала с `os error 32` (файл `search.exe` занят): в системе висели
    **11 незавершённых python-процессов** из `.venv` (это то самое зависание на выходе
    из заметки этапа 4 - оно копится). Снял их через `Get-Process | Where Path -like
    "*usprings_rag*" | Stop-Process -Force`. Если ingest/переустановка падают на
    занятом файле - смотреть висящие процессы.

### 2026-07-12. Этап 6 - Генерация ответа (6.1-6.3 сделано; 6.4 заблокирован)
- Статус: в работе (код готов и проверен; A/B по LLM заблокирован внешним лимитом)
- Что сделано (код): `llm.py` - клиент OpenRouter (OpenAI SDK + base_url), системный
  промпт (отвечать строго по фрагментам, честно признавать отсутствие информации),
  сборка пользовательского промпта из пронумерованных чанков с документом и страницами,
  чтение `usage` в лог; валидация `OPENROUTER_API_KEY` при создании клиента.
  `answer.py` - развилка поиск -> порог -> (отказ | генерация), источники собираются из
  метаданных чанков (дедуп по документу, страницы склеиваются), лог вердикта и времени.
  `api.py` - `POST /ask`, прогрев BGE-m3 в lifespan-хуке. `eval/run_answers.py` -
  прогон набора вопросов через полный сценарий (он же инструмент A/B). 3 unit-теста
  (источники, промпт), всего 13 зелёных.
- Проверка приёмки (пройдена, на доступной модели - см. блокировку ниже):
  - Покрытые вопросы: осмысленные пошаговые ответы с верными источниками
    («направление деятельности» 0.7339, «отгрузка без остатков» 0.7264 - 5 чанков,
    «лицензионность» 0.5918, «ключевые параметры» 0.7413).
  - Околодоменный непокрытый («инвентаризация склада в 1С», сходство 0.5466 - выше
    порога): LLM ответила «в предоставленных фрагментах информации нет» - вторая линия
    (промпт) работает ровно как задумано на этапе 5.
  - Вне-доменный («погода», «плов»): отказ за 0.2-0.3 с, LLM не вызывается.
  - `POST /ask`: релевантный вопрос - ответ 15.9 с с источниками; вне-доменный -
    `refused=true` за 0.2 с. Прогрев в lifespan отработал (первый запрос не платит
    за загрузку весов).
- **Подшаг 6.4 (A/B) НЕ ВЫПОЛНЕН - повторить позже** (см. раздел «Незакрытые хвосты»
  в начале документа). Обе плановые модели (`qwen3-next`, `gemma-4-31b`) всю сессию
  отдавали `429 rate-limited upstream` от разных провайдеров (Venice, Google AI Studio,
  OpenInference). Лимит не наш: ключ не free-tier, `usage: 0`, а соседняя
  `google/gemma-4-26b-a4b-it:free` с тем же ключом отвечала штатно - на ней и прогнан
  весь сценарий (она нужна была только чтобы доказать работоспособность кода, в выборе
  модели не участвует). Модель по умолчанию не меняли. Инструмент для повтора готов:
  `eval/run_answers.py --model <id>`. Детали - `open-questions.md` (запись 2026-07-12).
  Практический вывод: доступность бесплатного тира нестабильна - аргумент к платному
  тиру на проде.
- Правки по ходу:
  - 429 от LLM возвращался пользователю как голый `500`. Для пилота на бесплатном тире
    это воспроизводимая ситуация (поймали дважды), поэтому в `/ask` добавлен отдельный
    ответ `503` с понятным текстом («модель временно недоступна, повторите через
    минуту») - не защитное программирование, а обработка доказанного сценария.
  - Замечено к этапу 7: при ответе «информации нет» портал всё равно покажет источники
    найденных чанков - выглядит нелогично (см. `open-questions.md`).

### 2026-07-12. Этап 5 - Калибровка порога (5.4-5.6, этап завершён)
- Статус: сделано (этап 5 закрыт целиком)
- Что сделано: `eval/questions.yaml` - 25 вопросов (11 covered с ожидаемым документом,
  6 paraphrased - разговорные формулировки без терминологии документов, 8 irrelevant -
  от заведомо чужих тем до «трудных негативов» про 1С). `eval/run_eval.py` - recall@1 и
  recall@k по покрытым, распределения лучших сходств relevant vs irrelevant, анализ
  зазора. pyyaml добавлен в зависимости (`uv add --no-sync` + `uv pip install`, чтобы
  не будить пересинхронизацию torch).
- Проверка приёмки:
  - recall@1 = 17/17 (covered 11/11, paraphrased 6/6) - верный документ всегда первый;
    recall@5 тем более полный. Поиск на этом корпусе работает отлично.
  - Распределения: relevant 0.548-0.813, irrelevant 0.288-0.561. На первом прогоне
    (6 негативов) зазор был «чистым» (+0.027), но добавление двух околодоменных
    негативов («инвентаризация склада в 1С» 0.547, «права доступа в 1С» 0.561)
    показало пересечение: непокрытые 1С-вопросы неотличимы порогом от покрытых -
    корпус содержит ту же лексику (роли, склад, остатки).
  - Выбран `SIMILARITY_THRESHOLD=0.53`: держит весь релевантный хвост (min 0.548),
    режет всё вне-доменное (max 0.521 - «резервная копия базы 1С»). Проверено вживую:
    этот негатив при 0.5 проходил, при 0.53 получает отказ. Полное обоснование
    (асимметрия ошибок, роль промпта этапа 6 как второй линии) - в `open-questions.md`
    (запись 2026-07-12).
- Следствие для этапа 6: приёмку «отказ без вызова LLM» проверять вне-доменными
  вопросами; околодоменные непокрытые пройдут порог - честное «информации нет»
  на них обязан дать промпт (включить такой вопрос в проверку этапа 6).

### 2026-07-12. Этап 5 - Семантический поиск (5.1-5.3 сделано, калибровка впереди)
- Статус: в работе (5.1-5.3 сделано; 5.4-5.6 - калибровка, следующим шагом)
- Что сделано (код): `retrieval.py` - `search()`: `embed_query` -> top-k по косинусу
  (pgvector `cosine_distance`, join с `documents` за метаданными) -> `SearchHit` со
  **сходством** (`1 - расстояние`) и пометкой `above_threshold`. `SearchResult` несёт
  вердикт порога (`passed` по лучшему совпадению, `relevant` - кандидаты выше порога) -
  на этом этап 6 построит развилку «отказ | генерация». Лучшее сходство пишем в лог
  на каждый запрос (подшаг 5.2, копим картину распределения).
  `search_cli.py` (`uv run --no-sync search "вопрос"`, `--top-k`, `--threshold`,
  `--full`) - ручная проверка выдачи. 5 unit-тестов порога/метаданных
  (`tests/test_retrieval.py`), всего 10 тестов зелёные.
- Проверка приёмки (частичная, ручная - формальный recall@k будет на 5.5):
  - Релевантный вопрос «Как поменять направление деятельности в договоре?» - верный
    документ на 1-м месте, сходство **0.7404** (второй кандидат - 0.5300).
  - Заведомо нерелевантный «Какая погода в Астане на выходных?» - лучшее сходство
    **0.2875**, вердикт «ниже порога» (отказ).
  - Разделение relevant/irrelevant выглядит широким (0.74 против 0.29) - хороший знак
    для порога, но значение выбираем на данных eval (5.4-5.6), не по двум точкам.
    Текущий `SIMILARITY_THRESHOLD=0.5` остаётся стартовым, не откалиброванным.
- Заметки:
  - Добавление точки входа `search` в `pyproject.toml` требует переустановки пакета,
    а `uv sync` уходит качать torch. Обход: `uv pip install -e . --no-deps` -
    перегенерирует консольные скрипты, зависимости не трогает.
  - В CLI поднимаем уровень INFO только для логгера `usprings_rag`: `basicConfig` с
    INFO на root тянет десятки строк httpx/huggingface на каждый запуск.
  - Зависание процесса на выходе (см. запись про этап 4) не воспроизвелось.

### 2026-07-11. Ревизия планов после анализа (этапы 1-4 без изменений)
- Статус: сделано
- По итогам сверки планов с `overview.md` внесены правки в `mvp-plan.md` и
  `mvp-dev-plan.md` (реализованных этапов 1-4 не касаются):
  - Калибровка порога перенесена в этап 5 (подшаги 5.4-5.6), A/B LLM - в этап 6:
    приёмка этапа 6 зависит от откалиброванного порога.
  - Этап 7: новый подшаг 7.4 - раздача исходных PDF из FastAPI + нормализация
    `source_path` до относительного POSIX-пути (сейчас пишется как передан CLI).
  - Этап 8: torch CPU-колесом, монтирование HF-кэша хоста и папки инструкций
    в контейнер `app`.
  - Word (docx) внесён в пост-MVP список `mvp-plan.md` (в планах отсутствовал,
    хотя в `overview.md` назван основным форматом).
  - Заметки к этапам 5-6: сходство = 1 - расстояние (`<=>` в pgvector), прогрев
    модели эмбеддингов в lifespan, валидация `OPENROUTER_API_KEY` при создании
    клиента, логирование лучшей близости с этапа 5.
- Решения зафиксированы в `open-questions.md` (записи от 2026-07-11).

### 2026-07-11. Этап 4 - Ingest: эмбеддинги и запись в БД (завершено)
- Статус: сделано
- Веса BGE-m3 докачаны напрямую `curl.exe -C -` в snapshot-папку HF-кэша
  (в PowerShell важно звать именно `curl.exe`: голый `curl` - алиас
  Invoke-WebRequest с другим синтаксисом); sha256 совпал.
- Проверка приёмки: `uv run --no-sync ingest` записал 8 документов, 23 чанка,
  у всех заполнен `embedding` размерности 1024 (проверено SQL). Повторный прогон -
  все 8 файлов `[skipped]`, дублей нет (8 документов / 23 чанка без изменений).
- Заметки по запуску на Windows:
  - Русский вывод CLI падает с UnicodeEncodeError при перенаправлении stdout
    (консоль cp1252) - запускать с `PYTHONUTF8=1`; для живого прогресса в лог
    полезен и `PYTHONUNBUFFERED=1`.
  - Первый прогон после записи в БД не завершил процесс (завис на выходе
    интерпретатора, работа была сделана и закоммичена) - сняли вручную; на втором
    прогоне не воспроизвелось. Если повторится - разобраться (подозрение на
    torch/sentence-transformers при shutdown).

### 2026-07-11. Этап 4 - Ingest: эмбеддинги и запись в БД (в работе)
- Статус: в работе
- Что сделано (код): `embeddings.py` - интерфейс `EmbeddingProvider` (`embed_texts`,
  `embed_query`) + `BGEEmbeddingProvider` (ленивая загрузка BGE-m3, нормализация
  векторов). `ingest/pipeline.py` - `ingest_file`: парсинг -> чанкинг -> батч-
  векторизация -> запись `documents`+`chunks` в одной транзакции; идемпотентность по
  паре (source_path, content_hash): без изменений пропускаем, при изменении удаляем
  старый документ (каскад) и вставляем заново. `cli.py` - реальный ingest по
  умолчанию, `--dry-run` под флагом. `sentence-transformers` добавлен в зависимости.
- Заблокировано загрузкой весов, приёмка ещё не пройдена.
- Инфраструктурные заметки (важно, канал ~100 КБ/с, флаки):
  - Установка `sentence-transformers` тянет torch (cp314, колесо ~116 МБ). uv не
    умеет докачивать и рвался у конца скачивания torch -> бесконечные рестарты.
    Обход: скачали колесо torch напрямую `curl -C -` (докачка), проверили sha256,
    поставили `uv pip install <локальное колесо>`, затем `uv pip install
    sentence-transformers` дотянул остальной ML-стек. torch удовлетворён.
  - ВАЖНО про запуск: `uv run` по умолчанию делает авто-sync, а sync строго сверяет
    torch с lock (источник PyPI) и переустанавливает -> снова качает torch. Поэтому
    все команды запускаем как `uv run --no-sync ...` (иначе uv уходит качать torch).
  - Веса BGE-m3: у репозитория BAAI/bge-m3 НЕТ `model.safetensors`, веса в
    `pytorch_model.bin` (~2,27 ГБ / 2271145830 байт, sha256
    b5e0ce3470abf5ef3831aa1bd5553b486803e83251590ab7ff35a117cf6aad38). Мелкие файлы
    (config, tokenizer.json, sentencepiece, 1_Pooling) уже в HF-кэше. Крупный файл
    качаем `curl -C -` прямо в snapshot-папку HF-кэша (докачка переживает обрывы;
    hf-xet и стандартный hf-resume рвались/не возобновляли). При ~100 КБ/с - ~6 часов.
  - Модель грузим один раз при старте; кэш - стандартный HF (`~/.cache/huggingface`),
    для docker позже вынесем в volume (см. mvp-dev-plan этап 8).

### 2026-07-11. Этап 3 - Ingest: парсинг и чанкинг
- Статус: сделано
- Что сделано: пакет `ingest` - `pdf.py` (постраничное извлечение через pypdf,
  dataclass `Page`), `chunker.py` (секционное разбиение по абзацам с перекрытием,
  деление слишком длинных абзацев по предложениям + жёсткий рез как крайний случай,
  привязка page_from/page_to), `cli.py` (`uv run ingest [dir]` - сухой прогон со
  статистикой, без записи в БД). Точка входа `ingest` в `pyproject.toml`.
- Проверка приёмки: на 8 PDF текст читаемый, склеек нет, пустых страниц нет; число
  чанков разумное - 7 коротких документов дают 1-2 чанка, «Планирование отгрузки»
  (17 стр.) - 14 чанков; страницы проставлены. 5 unit-тестов чанкера (`tests/
  test_chunker.py`) зелёные (упаковка, перекрытие, деление длинных, диапазон страниц).
- Решения/заметки:
  - Шаг 3.2 (запасной `pdfplumber`) не понадобился: pypdf извлёк даже крупный файл
    со сложной вёрсткой и таблицами читаемо. Зависимость не добавляли (не
    переусложняем без доказанной необходимости).
  - Размер чанка меряем оценкой токенов (эвристика ~3 символа/токен, `CHARS_PER_TOKEN`),
    не точным токенизатором BGE-m3 - достаточно для контроля размера на сухом прогоне
    и не тянет тяжёлую модель на этап 3. Точный подсчёт при необходимости - на калибровке.
  - Первая версия давала чанки-выбросы 800-1100 токенов на страницах без пустых строк
    (титул, ОГЛАВЛЕНИЕ): исходный блок склеивался в один абзац. Добавили деление
    длинного абзаца по предложениям - выбросы ушли, чанки в пределах ~300-590 токенов.
  - Колонтитулы «Страница N из N» и подписи «Рисунок N» остаются в тексте (минорный
    шум, без OCR картинки не извлекаются) - при необходимости чистим на калибровке.
  - pytest добавлен как dev-зависимость (`[dependency-groups] dev`).

### 2026-07-10. Этап 2 - Схема БД и миграции (SQLAlchemy + Alembic)
- Статус: сделано
- Что сделано: ORM-модели `Document`, `Chunk` (`models.py`), тип `vector` через
  `pgvector.sqlalchemy.Vector`; `db.py` - engine + `SessionLocal`. Alembic настроен
  (`alembic.ini`, `alembic/env.py` берёт URL и metadata из приложения). Миграции:
  `0001` - расширение vector + таблицы, `0002` - HNSW-индекс (`vector_cosine_ops`,
  m=16, ef_construction=64) и B-tree по `collection`.
- Проверка приёмки: `alembic upgrade head` создаёт расширение, таблицы и индексы
  (проверено в psql); `downgrade base` -> `upgrade head` повторяемы; `alembic check`
  сообщает "No new upgrade operations detected".
- Правки по ходу:
  - `alembic.ini` читается configparser в локальной кодировке Windows (cp1252) -
    убрал кириллицу из ini (комментарий на английском), иначе UnicodeDecodeError.
  - `created_at` в модели явно `DateTime(timezone=True)`, чтобы совпасть с `timestamptz`
    в миграции (иначе `alembic check` видел расхождение типа).

### 2026-07-10. Этап 1 - Каркас и окружение
- Статус: сделано
- Что сделано: `uv`-проект (`pyproject.toml`, src-layout, пакет `usprings_rag`),
  зависимости через `uv add`; `config.py` (pydantic-settings, все ключи из `.env`);
  `docker-compose.yml` с сервисом `db` (образ `pgvector/pgvector:pg16`, healthcheck,
  volume `pgdata`). `.env`/`.env.example` дополнены ключами БД, эмбеддингов, поиска, LLM.
- Проверка приёмки: `docker compose up -d db` -> контейнер `healthy`; расширение
  `vector` доступно (v0.8.5); `config`/`models` импортируются, таблицы регистрируются
  в metadata.
- Решения/заметки:
  - `sentence-transformers` (тянет torch, несколько ГБ) на этапах 1-2 не используется -
    добавим на этапе 4, где он реально нужен (принцип инкрементальности).
  - На этой машине порт 5432 занят нативным Postgres Windows - публикуем контейнер на
    5433 (`POSTGRES_PORT=5433`, порт учтён в `DATABASE_URL`). Внутри контейнера 5432.
    В `.env.example` дефолт оставлен 5432 с примечанием.

<!-- старые записи ниже -->

